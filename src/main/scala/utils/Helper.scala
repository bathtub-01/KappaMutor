package utils

import chisel3._

object Helper {
  // TODO: design the sub-class later...
  // def multiModule[M <: Module, T <: Data](n: Int, m: M): Vec[T] = {
  //   val modules = Seq.fill(n)(m)
  //   val ports = Wire(Vec(n, m.io))
  // }
}